@model BinaryStudio.ClientManager.DomainModel.Entities.Inquiry

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.HiddenFor(model => model.Id)
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Inquiry</legend>
                
        <div class = editor-label>
            @Html.EditorFor(model => model.Issuer.FullName)
            @Html.ValidationMessageFor(model => model.Issuer.FullName)
        </div>
        
        <div class = editor-label>
            @Html.EditorFor(model => model.Issuer.Email)
            @Html.ValidationMessageFor(model => model.Issuer.Email)
        </div>

        <div class = editor-label>
            @Html.EditorFor(model => model.Issue.Subject)
            @Html.ValidationMessageFor(model => model.Issue.Subject)
        </div>
        
        <div class = editor-label>
            @Html.EditorFor(model => model.Issue.Body)
            @Html.ValidationMessageFor(model => model.Issue.Body)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
